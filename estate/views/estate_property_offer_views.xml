<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_estate_property_offer_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Offre de propriété immobilière">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_accept" string="Accepter" type="object" icon="fa-check"
                        attrs="{'invisible':[('status','!=', False)]}"/>
                <button name="action_refuse" string="Refuser" type="object" icon="fa-check"
                        attrs="{'invisible':[('status','!=', False)]}"/>
                <field name="status"/>
            </form>
        </field>
    </record>
    <record id="view_estate_property_offer_tree" model="ir.ui.view">
        <field name="name">estate.property.offer.tree</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <tree string="Offre de propriété immobilière" editable="bottom"
                  decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <button name="action_accept" string="Accepter" type="object" icon="fa-check"
                        attrs="{'invisible':[('status','!=', False)]}"/>
                <button name="action_refuse" string="Refuser" type="object" icon="fa-check"
                        attrs="{'invisible':[('status','!=', False)]}"/>
                <field name="status" invisible="1"/>
            </tree>
        </field>
    </record>
</odoo>